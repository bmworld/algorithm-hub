name: Update README stats

on:
  # 매일 KST 오전 6시에 자동 실행 (UTC 21:00 전날)
  # schedule:
  #   - cron: "0 21 * * *"
  workflow_dispatch:    # 수동 실행 버튼
  push:
    branches: [ main ]  # main 브랜치에서만 실행

permissions:
  contents: write       # README 갱신 커밋 권한

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # 전체 히스토리 가져와 rebase 충돌 방지

      - name: Generate total problem stats
        shell: bash
        run: |
          set -euo pipefail
          
          # ----------------------------------------------------
          # 경로: root/work/Java/백준, root/work/Kotlin/백준 등
          # ----------------------------------------------------
          count_dir () {
            local dir="$1" ext="$2"
            if [ -d "$dir" ]; then
              find "$dir" -type f -name "$ext" 2>/dev/null | wc -l
            else
              echo 0
            fi
          }

          # -------------------
          # 온라인 저지별 집계
          # -------------------

          # 백준
          baekjoon_java=$(count_dir "work/Java/백준" "*.java")
          baekjoon_kotlin=$(count_dir "work/Kotlin/백준" "*.kt")
          baekjoon_total=$((baekjoon_java + baekjoon_kotlin))

          # 프로그래머스
          programmers_java=$(count_dir "work/Java/프로그래머스" "*.java")
          programmers_kotlin=$(count_dir "work/Kotlin/프로그래머스" "*.kt")
          programmers_total=$((programmers_java + programmers_kotlin))

          # 총합
          total=$((baekjoon_total + programmers_total))

          # -------------------
          # README 콘텐츠 작성
          # -------------------
          export TZ=Asia/Seoul
          now="$(date '+%Y-%m-%d %H:%M %Z')"

          CONTENT_FILE="$(mktemp)"
          printf "%s\n" \
          "## 🚀 문제 집계 (${now})" \
          "- Baekjoon: ${baekjoon_total} 문제 (Java ${baekjoon_java}, Kotlin ${baekjoon_kotlin})" \
          "- Programmers: ${programmers_total} 문제 (Java ${programmers_java}, Kotlin ${programmers_kotlin})" \
          "- Total: ${total} 문제" \
          > "$CONTENT_FILE"

          START="<!-- TOTAL_PROBLEM_STATS_START -->"
          END="<!-- TOTAL_PROBLEM_STATS_END -->"

          if grep -q "$START" README.md && grep -q "$END" README.md; then
            # 마커 사이 내용을 임시파일로 교체 (GNU sed)
            sed -i -e "/$START/,/$END/{
              /$START/{p;r $CONTENT_FILE}
              /$END/p
              d
            }" README.md
          else
            {
              echo ""
              echo "$START"
              cat "$CONTENT_FILE"
              echo "$END"
            } >> README.md
          fi

          rm -f "$CONTENT_FILE"

      - name: Commit & push if changed
        shell: bash
        run: |
          if ! git diff --quiet; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add README.md
            git commit -m "chore(readme): auto-update problem stats"
          
            # 원격 rebase 후 push (충돌 방지)
            git pull --rebase origin main || true
            git push
          else
            echo "✅ No changes detected. Nothing to commit."
          fi
