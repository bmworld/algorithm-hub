package 프로그래머스.Lv0.홀짝구분하기

fun main(args: Array<String>) {
  val x = readLine()!!.toInt()
  print("$x is ${if (x % 2 == 0) "even" else "odd"}")
}

/**
 * ```
 * [ME]
 * ```
 *
 *
 * ```
 * [RIVAL]
 * ```
 */
fun main() {
//  val s = Solution()
//  validate(s.solution(-4, 7, true), 3)
//  validate(s.solution(-4, 7, false), -11)
}
